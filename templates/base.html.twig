<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-montserrat text-sm bg-white">
    <div class="flex min-h-screen">
        {% include 'parts/structure/left-menu.html.twig' %}
        <main class="flex-1">
            {% for message in app.flashes('success') %}
                <div class="bg-green-500 text-white px-6 py-3 text-sm">{{ message }}</div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="bg-red-600 text-white px-6 py-3 text-sm">{{ message }}</div>
            {% endfor %}
            {% block content %}{% endblock %}
        </main>
    </div>
</body>
</html>